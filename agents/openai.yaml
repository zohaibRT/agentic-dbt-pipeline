interface:
  display_name: "dbt Pipeline"
  short_description: "Build dbt projects with agents"
  default_prompt: "Use $agentic-dbt-pipeline to set up this dbt project with staged commits, validation, docs, CI, and Agents Schema."

policy:
  allow_implicit_invocation: true
