# Copy to .env and fill only the values you want the agent to reuse.
# Do not put passwords, tokens, or private keys here.

DBT_DOMAIN=hospital
DBT_PROFILE_NAME=shopsphere_analytics_dbt
DBT_SOURCE_SCHEMA=doctors_hospital_src
DBT_SOURCE_NAME=doctors_hospital_src
DBT_LAYER_SCHEMA_PREFIX=doctors_hospital_src
DBT_GITHUB_REPO_NAME=local-only

DBT_LAYER_1=bronze
DBT_LAYER_2=silver
DBT_LAYER_3=gold

DBT_COMMIT=ask
DBT_PUSH_TO_GITHUB=false
DBT_MATERIALIZATION_PROFILE=prod
DBT_AUTO_AGENTS_SCHEMA=true
