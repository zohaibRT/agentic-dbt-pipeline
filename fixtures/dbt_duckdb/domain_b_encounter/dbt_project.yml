name: domain_b_encounter
version: '1.0.0'
config-version: 2
profile: fixture_duckdb
model-paths: ["models"]
seed-paths: ["seeds"]
target-path: target
clean-targets:
  - target
  - dbt_packages
seeds:
  +schema: main
models:
  domain_b_encounter:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    gold:
      +materialized: table
