# dbt Analytics Engineer Prompt

Install once:

```bash
npx skills add zohaibRT/agentic-dbt-pipeline
```

Open your dbt project workspace, then use the prompt below. You do **not** need to create `.env` manually before the first run; the agent creates workspace `.env` from `.env.example` when it is missing.

## Recommended Prompt

```text
Use the dbt Analytics Engineer skill (`agentic-dbt-pipeline`).

Act as a Principal Data Engineer. Design the dbt project to be modular, idempotent, scalable, testable, cost-efficient, and safe for downstream consumers. Treat data asset development like software engineering.

Build the dbt project using the settings from `.env`.
Run the full pipeline from source discovery through final delivery.

Read any project knowledge files I provide, such as `AGENT_KNOWLEDGE.md`, `docs/dbt_knowledge.md`, `docs/business_rules.md`, `.agents/project_knowledge.md`, and `reports/agent/CONTEXT_TREE.md`. Apply `project_rules` from my prompt first, summarize which knowledge was used in each phase plan/report, and ask before persisting new dbt knowledge from chat into project files.

Use the skill's built-in knowledge layer for reusable dbt, big data, warehouse optimization, semantic layer, Power BI, privacy, and validation standards. Use project knowledge only for local/domain overrides. Do not copy large external documentation into the project; use official documentation only when current version-specific behavior is needed.

If `.env` is missing, resolve `.env.example` from the workspace root, dbt project root, or installed skill folder, then create a safe local workspace `.env` from that template with placeholder values only, ask me which dbt profile this pipeline should use by listing available profiles from `~/.dbt/profiles.yml` with profile name, adapter, database or database-equivalent, and profile schema, stop before discovery or dbt commands, and ask me for DBT_DOMAIN, DBT_PROFILE_NAME, and DBT_SOURCE_SCHEMA. You may also ask for optional DBT_BUSINESS_DESCRIPTION when more business context would help analytics insight reporting, but do not require it for discovery. Do not create discovery reports while `.env` is missing or placeholder-only. Do not fill `.env` from a profile, profile target schema, warehouse schema, previous run, example, or guess. Do not use DBT_DOMAIN or DBT_BUSINESS_DESCRIPTION directly as project folders, model folder slugs, warehouse schema prefixes, database names, or source names; derive those from source/project evidence unless I explicitly override them. Do not say you will resolve the active profile or run PostgreSQL, Redshift, Snowflake, BigQuery, Databricks, or any adapter-specific discovery until I choose `DBT_PROFILE_NAME`. Do not search the repository, inspect terminal output, infer, suggest, or summarize values from other workspaces or previous runs. Do not choose a profile automatically.

First, perform lightweight read-only project discovery only: inspect source schemas/tables, write `reports/agent/00_discovery/discovery_report.md`, `reports/agent/00_discovery/requirements.md`, and reusable SQL proof files under `reports/agent/00_discovery/sql_proofs/`, update `reports/agent/PIPELINE_STATUS.md` and `reports/agent/CONTEXT_TREE.md`, summarize what you conclude about the data project, and ask whether I want to add requirements. The requirements file must capture source-derived requirements, evidence, confidence, recommended defaults, open questions, user-provided requirements, and deferred or blocked scope. Discovery SQL proofs must include runnable queries and captured results for table inventory, per-table row counts, candidate keys, important statuses such as active/open/closed counts, date coverage, numeric summaries, and relationship/cardinality checks wherever the source supports them. Before discovery, resolve the active dbt profile from `.env`, read that profile's adapter from `~/.dbt/profiles.yml`, announce the selected profile and adapter, and use only that adapter's discovery path. Do not call AWS, Redshift, PostgreSQL, Snowflake, BigQuery, Databricks, cloud identity checks, warehouse connectors, metadata queries, or Model Context Protocol discovery servers before `.env` and the selected dbt profile adapter are resolved. Do not query AWS, Redshift, or any other warehouse unless the selected dbt profile uses that adapter or I explicitly ask you to change profiles. If the configured source is missing, empty, inaccessible, ambiguous, mismatched, or appears wrong, stop after metadata-only candidate listing, recommend the likely replacement with evidence, and wait for my approval before changing database, dataset, catalog, schema, table, tenant, client, domain, environment, assumption, `.env`, profile settings, profiling candidate tables, writing discovery reports, or continuing discovery. Keep discovery output focused on source data, entities, relationships, data quality, candidate models/metrics, and open modeling decisions; keep setup details out of the discovery summary except for a short inputs-used note. Include a `Recommended Medallion Direction` section that covers sources, bronze/staging, silver/intermediate, and gold/marts with recommendation, evidence, not-ready items, and approval needs for each area. During discovery, create a Mermaid entity relationship diagram when credible relationships exist, and create any other necessary Mermaid diagrams such as source inventory, candidate business process flow, or high-level medallion direction when they help review the project. Do not fully design every layer upfront; run focused discovery again before sources, bronze, silver, gold, semantic, evaluator, documentation, and analytics insight reporting phases.

Use Mermaid for every diagram. For entity relationships, use Mermaid `erDiagram`. Verify every Mermaid diagram you add or change is visible/parseable, and record the result in the phase report.

Use full wording in all user-facing plans, reports, summaries, diagram notes, and final handoffs. Avoid shorthand such as primary key abbreviations, foreign key abbreviations, entity relationship diagram abbreviations, documentation abbreviations, repository abbreviations, and continuous integration abbreviations unless quoting a command, filename, package name, environment variable, or official tool name.

Every report, final handoff, business-facing presentation, and Power BI report plan must be actionable, not a data dump. Include five report pillars when relevant: context and strategy, key performance indicators, trend analysis and variance, insights and attribution, and recommendations and next steps. If a pillar is not supported by validated data yet, show it as deferred or needing business input with the reason. Do not invent targets, benchmarks, root causes, or recommendations without evidence. Use the universal analytics hierarchy: business -> department/business area -> business process -> key performance indicators -> metrics -> measures -> dimensions -> facts. During analytics insight reporting, maximize useful report results by creating business process, fact, dimension, measure, metric, and key performance indicator catalogs from validated data, while keeping uncertain items deferred or blocked with proof status. For Power BI reports and Matplotlib web reports, use a rich interactive canvas standard on main pages: header/navigation with report title, page title, last refreshed timestamp, reset filters or refresh control, and page navigation; prioritized executive key performance indicator cards at the top; visible primary slicers or filters; trend and comparison visuals in the middle; driver, exception, and segmentation panels; matrix/detail visuals at the bottom or on a details page; and smart tooltips or drill-through pages where safe and supported. Analyze the maximum useful supported key performance indicators, place only the highest-priority measures in the top executive row for readability, and put supporting key performance indicators in a scorecard/details area or Report Information page. Use a professional polished theme with intentional color palette, readable spacing, captions, cards, callouts, and business-friendly labels. Include a Report Information, Report Settings, or About This Report page with report purpose, audience, data source, refresh details, page guide, key performance indicator definitions, filter definitions, caveats, privacy handling, validation summary, and open decisions. Document any missing canvas element with the reason.

After I answer at the discovery checkpoint, run project setup and configuration automatically: write/update `AGENT_PLAN.md` with the setup phase marked as automatic setup-only, create the local dbt scaffold if needed, install missing dependencies, run `dbt debug`, run `dbt deps`, run `dbt parse`, and write the setup report. Interpret my response by the active workflow checkpoint, not by general intent. Discovery checkpoint approval only confirms the source and automatic setup. It does not approve source YAML generation, bronze/staging, silver/intermediate, gold/marts, semantic layer, evaluator, documentation, analytics insight reporting, presentation layer, continuous integration, Agents Schema, commits, pushes, or source switching. Stop and ask first if required settings are missing, the selected profile is unsafe or failing, existing project files would be overwritten, warehouse objects would be created or replaced, credentials are needed, or I explicitly disabled automatic setup.

During project setup and configuration, perform profile target schema hygiene and write the result into `reports/agent/01_setup/setup_report.md` and `reports/agent/PIPELINE_STATUS.md`: active profile, adapter, database, target schema, source schema, whether the target schema is safe, and any required mitigation. Treat unsafe target schema routing as a setup blocker, not an optional follow-up.

At the start of project setup and configuration, install the skill `requirements.txt` when available and run `scripts/create_report_skeleton.py --root <project.root-or-workspace.root>` from the installed skill or workspace. The skeleton must create every SQL proof folder with `_proof_index.md` files before validation queries are written. If requirements installation or skeleton creation is skipped, document the reason in the setup report and pipeline status.

Treat the configured source schema and source tables as read-only and immutable for the entire run. Never run `UPDATE`, `INSERT`, `DELETE`, `TRUNCATE`, `MERGE`, `CREATE`, `DROP`, `ALTER`, backfill, repair, or status-changing commands against source. If I ask to change source data, such as marking records complete, implement the requirement only in dbt framework artifacts in non-source schemas, such as derived model columns, flags, mappings, tests, audits, or snapshots, and state that the source was not changed.

After every bronze/staging, silver/intermediate, and gold/marts build, run warehouse data validation queries before moving to the next layer. Verify row counts, expected-empty evidence, grain, keys, relationships, cardinality, row-count movement, row loss, row multiplication, date coverage, status/category distributions, important measures, mapping coverage, and privacy exposure. Save every validation query as a reusable SQL proof file under the current phase `sql_proofs/` folder with purpose, expected result, captured result, status, and runnable SQL. Write `Data Verification Results`, `SQL Proof Files`, and cardinality/grain evidence into the layer report, share the important results with me, and stop when a model expected to contain data is empty or any validation issue is unexplained.

For gold/marts, semantic layer, analytics insight reporting, presentation layer, and final delivery, keep a clear hierarchy: measures are raw counts/sums/averages/minimums/maximums, metrics add time/dimension/ratio/rate/average/ranking/aging context, and key performance indicators are decision-relevant metrics tied to goals, thresholds, targets, risks, or management review. During analytics insight reporting, create as many useful supported measures as the validated data safely allows in `measure_catalog.md`, promote supported measures into contextual metrics in `metric_catalog.md`, and only then promote the strategic, validated subset into `kpi_catalog.md`. Also create `business_process_catalog.md`, `fact_catalog.md`, and `dimension_catalog.md` so reporting is organized by business area, process, event, fact, dimension, and safe slicer/drill-down. Define measures, metrics, and key performance indicators with business meaning, source model, grain, formula, filters, time field, dimensions, caveats, SQL proof file, captured result, approval status, and metric verification status. Discover candidates with a generic, domain-neutral framework: classify tables, detect grain, detect count/amount/quantity/status/date/ranking candidates, map candidates to volume, value, average value, ratio, funnel, conversion, status mix, time trend, cycle time, aging, ranking, utilization, quality, retention, or exception archetypes, then score confidence as `HIGH`, `MEDIUM`, `LOW`, or `BLOCKED`. Do not create key performance indicators from column names alone. Defer or ask for approval when definitions are ambiguous; do not silently implement advanced metrics. For every implemented key performance indicator, reconcile expected versus actual numerator, denominator, filter logic, grain, cardinality, row multiplier, row loss, and final result from source to bronze/staging, silver/intermediate, gold/marts, semantic metrics, and Power BI/DAX/report measures. Stop before semantic layer, analytics insight reporting, presentation layer, or final delivery when a metric does not reconcile, including cases where a denominator accidentally equals the numerator but the business definition expects additional states, source-to-final variance is unexplained, grain/cardinality proof is missing, or the SQL proof file is missing.

After marts, semantic layer, evaluator, and documentation are complete, run analytics insight reporting before presentation work. Read `references/analytics-insight-reporting.md`, `references/report-artifact-organization.md`, `references/universal-analytics-framework.md`, `references/kpi-discovery-framework.md`, `references/kpi-reconciliation.md`, and `references/cardinality-validation.md`, write/update `AGENT_PLAN.md`, get my approval, and produce managed report outputs under `reports/agent/09_analytics_insights/`, measure/metric/key performance indicator outputs under `reports/agent/09_analytics_insights/kpis/`, SQL proofs under `reports/agent/09_analytics_insights/kpis/sql_proofs/`, plus root `reports/agent/REPORT_INDEX.md` and `reports/agent/HUMAN_VERIFICATION_GUIDE.md`. For older flat-layout projects, read legacy files from `reports/agent/` only when canonical files are missing, and do not move existing files without my approval. Do not create fake insights, suggest charts just because data exists, or build presentation artifacts in this phase. Every business process, fact, dimension, measure, metric, key performance indicator, and report candidate must map to validated marts or semantic metrics and include a SQL proof file or a documented blocked/deferred reason where applicable. Promote only `HIGH` confidence and user-approved `MEDIUM` confidence strategic key performance indicators with source-to-current-layer reconciliation plus grain/cardinality proof into `kpi_catalog.md`, semantic metrics, or Power BI DAX; keep useful non-strategic measures and metrics in `measure_catalog.md` and `metric_catalog.md`; defer `LOW`, `BLOCKED`, and unreconciled candidates to `insight_backlog.md`. Clearly separate trusted outputs from uncertain or deferred outputs. Maximum means maximum useful business insight supported by validated data: business areas, processes, facts, dimensions, measures, metrics, key performance indicators, report pages, and deferred opportunities with proof status, not maximum number of dashboards.

After analytics insight reporting is complete, ask whether I want a presentation layer and which technology to use before closing final delivery. Use the canonical analytics insight files under `reports/agent/09_analytics_insights/` and `reports/agent/09_analytics_insights/kpis/` as the scope contract, falling back to legacy flat `reports/agent/` files only when canonical files are absent. The presentation decision must be shown as a concise evidence summary and, when the runtime supports it, a native clickable question. The question should say that analytics insight reporting is complete, list the most important validated key performance indicators with values, recommend a Matplotlib refreshable web report as the default presentation technology, mention Power BI as the interactive alternative, state the recommended page set, mention important blockers/caveats, and ask: `Do you want a presentation layer, and which technology should I use?` Use these options: `Yes - build Matplotlib refreshable web report (recommended)`, `Yes - prepare Power BI Desktop template handoff`, `No presentation layer - complete final delivery now`, and `Tell me what to change first`. Do not invent pages, key performance indicators, visuals, or business scope that bypass analytics insight reporting unless I explicitly override it. If I approve the presentation layer and do not specify another technology, default to the Matplotlib refreshable web report workflow: read `references/matplotlib-presentation-layer.md`, install missing `matplotlib`, `numpy`, and `pandas` when needed, create `serve_report.py`, `data_access.py`, `report_builder.py`, modular `report_pages/`, `report_theme.py`, `report_theme.md`, `open_report.bat`, `requirements-matplotlib.txt`, `report_spec.md`, `kpi_figure_coverage.md`, `label_dictionary.md`, `sql_verification/`, and a polished local `report.html` shell with colorful tabs, summary cards, chart cards, captions, caveats, validation status, refresh timestamp, and refresh control. Render SQL-verified charts as live Matplotlib SVG/HTML endpoints or approved browser-native charts from refreshed JSON. Do not use PNG as the primary web rendering path; PNG/SVG files are optional exports or snapshots only. Map every recommended measure, metric, and key performance indicator from `measure_catalog.md`, `metric_catalog.md`, `kpi_discovery_matrix.md`, and `kpi_catalog.md` into the web report or an explicit blocked/deferred note. If I explicitly choose Power BI, use the Power BI Desktop human-connected template workflow: create the Power BI handoff folder, state the exact `.pbip` path and filename to save, list approved gold/semantic tables, relationship rules, storage mode, and the required measures table, then stop until I confirm the PBIP is saved and data is attached. After I confirm the PBIP path, inject only approved DAX measures, descriptions, format strings, display folders, and safe annotations into the approved measures table, then validate. Do not generate source partitions, Power Query M, relationships, visuals, or a full PBIP semantic model unless I explicitly approve generated PBIP mode or report-page editing. For approved presentation work, still produce a professional enterprise plan from validated facts, dimensions, semantic metrics, source profiling, and data quality evidence; include context and strategy, executive overview, key performance indicators, trend analysis and variance, insights and attribution, financial or value views, operations or activity views, entity performance, segmentation, exceptions, recommendations and next steps, and detail or drillthrough pages only when supported by the data. Maximize validated, decision-useful information, not every available column. Do not ask me to design every page or visual, but do ask for decisions that affect business meaning, privacy, cost, refresh behavior, or downstream usability. Treat presentation content as two visual layers: domain key performance indicators and a standard time showcase. If validated facts have usable date columns, the approved presentation plan must include a `Trends` page or equivalent standard time showcase with last calendar year, year to date, last 12 months, by-year, and by-month visuals for primary facts. Discover time fields from gold facts, model YAML, semantic models, or mart SQL; do not hardcode field names from one domain. Use governed measures or reportable filters where they exist, validate every time visual number with SQL, and record each exact verification query in `reports/agent/10_presentation/presentation_report.md`. Verify every key performance indicator visual and DAX/report measure against gold or semantic SQL, including expected numerator, actual numerator, expected denominator, expected result, actual result, grain proof, and cardinality proof. Create a separate `presentation_layer` phase plan with page rationale, source models, measures, slicers, privacy handling, blocked visuals, refresh mode, blocked exports, and verification queries; build only the approved artifact/scope, validate it, and write `reports/agent/10_presentation/presentation_report.md`. Do not create dashboards, reports, slides, notebooks, generated PBIP models, or business intelligence artifacts unless I approve that follow-up work.

For approved Matplotlib work, use the official Matplotlib user guide at https://matplotlib.org/stable/users/index when figure structure or API behavior is uncertain. Before generating charts, run `python -c "import matplotlib, numpy, pandas"` and install any missing packages with `python -m pip install matplotlib numpy pandas` plus the warehouse client required by the active dbt profile when Python query execution is needed. Build a local refreshable web report as the primary artifact with `serve_report.py`, `report.html`, colorful classified tabs, summary cards, chart cards, captions, caveats, validation status, modular `report_pages/` Python modules, `open_report.bat` for Windows review, `label_dictionary.md` for business-friendly labels, and a comfortable rich theme via `report_theme.md`, `report_theme.py`, and custom CSS instead of default gray matplotlib styling or unstyled HTML. Use live Matplotlib SVG/HTML endpoints or approved browser-native charts from refreshed JSON so changed values appear after refresh. Validate the page itself before handoff with `scripts/validate_local_web_report.py`, proving that `http://127.0.0.1:<port>/` returns HTTP 200 and non-empty HTML; if it fails with `ERR_EMPTY_RESPONSE` or similar, fix the server/launcher and rerun validation before marking presentation complete. Do not stop at generated image files or use PNG as the primary chart rendering path.

For approved Power BI PBIP/TMDL artifacts, use the human-connected Desktop template workflow. The agent must create the Power BI handoff folder and checklist, tell me the exact `.pbip` path and filename to save, and wait until I confirm that the PBIP is saved and data is attached. The agent must not generate source partitions, Power Query M, relationships, visuals, or a full semantic model unless I explicitly approve generated PBIP mode or report-page editing. After I confirm the PBIP path, inject only approved DAX measures, descriptions, format strings, display folders, and safe annotations into the approved measures table such as `_KPI_Measures` or `_Measures`. Power BI measures must be generated only from `reports/agent/09_analytics_insights/kpis/kpi_catalog.md`, validated dbt semantic metrics, or explicit user-approved requirements; do not invent Power BI-only key performance indicators, denominators, business flags, keys, or relationship shortcuts. Run validation before handoff: detect and record the target Power BI Desktop version when Desktop validation is expected, check Power BI Modeling Model Context Protocol tool availability, run static validation, run relationship ambiguity audit, run a DAX smoke query when available, and run Power BI Desktop open validation when available. Treat Desktop open errors, invalid TMDL, generated PBIP metadata errors, missing calculated table `sourceColumn`, and duplicate annotation merge errors as failed validation. If generated PBIP mode is explicitly approved, use the bundled neutral PBIP template and run the same validation loop. Do not report the presentation layer as complete unless `reports/agent/10_presentation/presentation_report.md` and `PIPELINE_STATUS.md` show PASS with evidence, or clearly show BLOCKED/NOT RUN for unavailable required checks.

The presentation-layer recommendation and my decision are required for full pipeline final delivery. If I have not answered the presentation question, set status to `Analytics insight reporting complete - presentation decision pending`, not `Delivery complete`. If I approve a presentation artifact, validation must be completed before final delivery; if validation fails or cannot run, mark presentation and final delivery as blocked with evidence instead of complete. If you cannot produce the recommendation, mark it blocked or skipped with the reason in the final report, pipeline status, context tree, and final response.

Before final delivery, perform an advanced data-engineering review covering source lock, schema hygiene, layer validation, grain, tests, data quality, privacy, key performance indicators, semantic layer, evaluator, documentation, analytics insight reporting, presentation-layer recommendation, and operations.

After discovery, create `reports/agent/00_discovery/DISCOVERY_APPROVAL_CHECKLIST.md` and `reports/agent/REQUIREMENTS_TRACEABILITY_MATRIX.md` using the skill reference checklists. Do not continue to bootstrap/build until the discovery checklist decision is `APPROVED` or `APPROVED WITH CONDITIONS`. After every layer or phase, update `reports/agent/LAYER_VERIFICATION_LEDGER.md` with row count, grain, key, relationship, measure, SQL proof path, and PASS/WARN/FAIL/BLOCKED status for important models.

Use the Evidence-Driven dbt Build Process: the agent is allowed to build only what it can prove. `dbt build` proves that the pipeline runs, but it does not prove business correctness. No model, measure, metric, key performance indicator, dashboard value, or phase can be marked complete unless it has a verification record with business definition, source mapping, dbt model or report location, SQL proof file, expected result, captured result, difference or tolerance, PASS/WARN/FAIL/BLOCKED status, and an explanation of why the result is correct.

Maintain `reports/agent/KPI_DEFINITION_CONTRACTS.md` and `reports/agent/METRIC_VERIFICATION_MATRIX.md`. Every approved or proposed key performance indicator must have a contract with business meaning, formula, grain, date basis, included rows, excluded rows, source tables/models, built artifact, SQL proof path, expected result, actual result, difference/tolerance, approval status, and verification status. Every important measure, metric, and key performance indicator must reconcile source proof to mart proof, and semantic/presentation proof when those layers exist.

Before final delivery, run `python scripts/run_acceptance_gate.py --root .`, `python scripts/check_requirement_traceability.py --root .`, `python scripts/check_layer_proof_coverage.py --root .`, and `python scripts/verify_metric_reconciliation.py --root .`; record `reports/agent/ACCEPTANCE_GATE_REPORT.md`. Do not claim delivery complete if any hard gate returns FAIL. Then run a fresh independent verifier agent with `agents/dbt-verifier-agent.md` from zero builder chat context and write `reports/agent/INDEPENDENT_VERIFICATION_REPORT.md` plus `.json`. Do not claim delivery complete if independent verification returns FAIL.

Before each non-setup build phase, write/update `AGENT_PLAN.md`, explain in Markdown what you will build, and wait for my approval before implementing that phase. Treat each approval as checkpoint-scoped only. After a phase completes, write the phase report in the managed folder from `references/report-artifact-organization.md`, update `reports/agent/REPORT_INDEX.md`, share validation results, write/update `reports/agent/NEXT_PHASE_PROMPT.md` with the exact prompt for the recommended next phase, then show me a chat control-panel summary before asking approval. The chat summary must explain what was completed, what passed/warned/failed, what is recommended next, what the next phase will and will not include, and paste the exact next-phase prompt in chat. Do not only say "the prompt is in NEXT_PHASE_PROMPT.md." Ask approval through a native interactive question when the runtime supports it. Use the question "Do you want me to run this next-phase prompt as written?" with "Yes, run this prompt" as the recommended option, plus "Tell me what to change" and "Not now" when available. If interactive questions are unavailable, use the text fallback: "Do you want me to run this next-phase prompt as written? Reply Yes to proceed, or tell me what to change." Natural replies such as Yes, Proceed, Approved, Continue, Run this prompt, Looks good, or Go ahead approve only the displayed next-phase prompt for the active checkpoint. If I ask for any change to scope, models, key performance indicators, privacy, schemas, validation, materialization, or files, revise `AGENT_PLAN.md` and `reports/agent/NEXT_PHASE_PROMPT.md` before proceeding. Do not treat silence as approval and do not require exact phrases such as approve sources, approve bronze, approve silver, or approve gold after the next-phase prompt has been shown.

When I approve a displayed next-phase prompt, do not run `reports/agent/NEXT_PHASE_PROMPT.md` in isolation. First reload the phase context bundle: `SKILL.md`, `prompt.md`, phase-specific references, `AGENT_PLAN.md`, `reports/agent/PIPELINE_STATUS.md`, `reports/agent/CONTEXT_TREE.md`, `reports/agent/00_discovery/requirements.md` when present, legacy `reports/agent/requirements.md` only when the canonical file is absent, the latest relevant phase report, `reports/agent/NEXT_PHASE_PROMPT.md`, and project knowledge files when present. Then execute only the approved next phase.

In each discovery summary and phase plan, recommend the best next path with evidence, show what looks right, what is not ready yet, confidence about proven vs uncertain items, and what needs my approval. Do not ask me to design everything from scratch.

When sensitive fields or unclear coded fields appear, propose the safe default instead of only asking me what to do. For direct identifiers such as patient names or medical record numbers, recommend excluding, masking, or hashing them from gold/marts by default. For ambiguous, placeholder, abbreviated, generic, or poorly named fields, recommend passing them through bronze/staging as raw unmapped source fields, deferring mapping seeds until definitions are provided, and excluding them from gold/marts unless I approve raw audit exposure. Do not rename unclear fields unless I provide definitions or explicitly ask for suggested names; if I ask for suggestions, analyze values, propose candidate names with confidence, and wait for my approval of the exact final names before changing SQL or YAML.

In each phase plan, include a Data Engineer Decision Check covering grain, keys, joins, bridge tables, mappings, metrics, privacy, materialization, tests, and validation evidence. Ask me before guessing any decision that affects business meaning, privacy, correctness, cost, or downstream usability.

If you cannot properly understand source tables, relationships, business processes, required metrics, data quality rules, required output models, or reporting needs, do not assume. Ask me for the missing business meaning or approval, and defer dependent models, tests, metrics, semantic definitions, or presentation outputs until I confirm.

After each completed or blocked checkpoint, write/update the canonical phase report path from `references/report-artifact-organization.md`, `reports/agent/PIPELINE_STATUS.md`, `reports/agent/CONTEXT_TREE.md`, and `reports/agent/NEXT_PHASE_PROMPT.md` with what was done, what passed, warnings/failures/skips, assumptions, user decisions, phase outputs, report links, open decisions, and the exact next-phase prompt. For new projects, do not write phase-specific reports directly under `reports/agent/`; keep only root control-plane files there. Also share a short chat result summary with current checkpoint, status, report path, goal, what was completed, what was built or changed, validation, included scope, not-included scope, open decisions, next checkpoint, next goal, next includes, next does not include, known caveats, files/reports that will be created next, the exact next-phase prompt pasted in chat, and the interactive approval question or text fallback.

Ask me only when required `.env` values are missing, credentials or secrets are needed, automatic project setup hits a safety gate, a business rule is unclear, non-setup phase build approval is needed, or before committing, pushing, or changing schema behavior.
```

## Optional Project Rules

Add this only when the project has business-specific rules, mappings, joins, or privacy requirements.

```text
Project rules:
- Field mappings:
  - <source_table.source_column> -> <target_column>: <meaning/rule>
- Joins:
  - <left_table.column> -> <right_table.column>: <relationship>
- Metrics:
  - <metric_name>: <definition, grain, filters>
- Exclusions:
  - <tables/columns/records to ignore>
- Privacy:
  - <personally identifiable information / protected health information handling, masking, or exclusion rules>
- Naming:
  - <custom naming conventions>
- Special instructions:
  - <anything else the agent must follow>
```

If a rule is unclear, the agent should ask before modeling it.

## Single Phase

Use this only when you want one part of the workflow:

```text
Use the dbt Analytics Engineer skill (`agentic-dbt-pipeline`).

Use settings from `.env`.
workflow_phase: <init | sources | staging | intermediate | marts | semantic_layer | project_evaluator | docs | analytics_insight_reporting | presentation_layer | ci | agents_schema>

Use `docs` and `ci` only as exact workflow phase values. In explanations, write `documentation` and `continuous integration`.

Before building this phase, explain the plan in Markdown with your recommendation, evidence, confidence, risks, and approval needs, then wait for my approval. After it completes, write/update the phase report, pipeline status, context tree, and `reports/agent/NEXT_PHASE_PROMPT.md` files, then show the exact next-phase prompt and ask whether to run it as written using an interactive question when available.
```

Example for analytics insight reporting only:

```text
Use the dbt Analytics Engineer skill (agentic-dbt-pipeline).
Use settings from .env.
workflow_phase: analytics_insight_reporting
```
